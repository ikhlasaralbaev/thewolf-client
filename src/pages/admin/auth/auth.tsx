import { LoginForm } from '@/features/auth'

const Auth = () => {
	return (
		<div className='flex items-center justify-center h-screen bg-grayBg'>
			<LoginForm />
		</div>
	)
}

export default Auth
